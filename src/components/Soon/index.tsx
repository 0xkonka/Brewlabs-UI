const Soon = ({ className }: { className?: string }) => (
  <div
    className={`${className} absolute -right-3 -top-2 z-10 flex h-4 w-10 items-center justify-center rounded-[30px] bg-primary font-brand text-xs font-bold text-black`}
  >
    Soon
  </div>
);

export default Soon;
